<div class="form-horizontal well">
  <firldset>
  
<%= form_for(@testbb) do |f| %>
  <% if @testbb.errors.any? %>
    <div class="alert alert-error">
      <h4 class="alert-heading">
      <%= pluralize(@testbb.errors.count, "error") %> prohibited this testbb from being saved:
      </h4>
      <ul>
      <% @testbb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="control-group">
    <%= f.label :name, class:'control-label' %>
    <div class="controls">
        <%= f.text_field :name, class:'input-xlarge' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :message, class:'control-label' %>
    <div class="controls">
        <%= f.text_area :message, class:'input-xlarge', :size => "20x8" %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit class:'btn btn-primary' %>
  </div>
  </firldset>
<% end %>
</div>
